name: Build Windows Native App

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - 'windows-native/**'
      - '.github/workflows/build-windows.yml'

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore windows-native/ArkadasOdasi.Native.csproj

      - name: Publish portable EXE
        run: dotnet publish windows-native/ArkadasOdasi.Native.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:EnableCompressionInSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o publish/ArkadasOdasi-win-x64

      - name: Add server.txt placeholder
        shell: pwsh
        run: |
          if (!(Test-Path publish/ArkadasOdasi-win-x64/server.txt)) {
            Copy-Item windows-native/server.txt.example publish/ArkadasOdasi-win-x64/server.txt
          }

      - name: Upload Windows app
        uses: actions/upload-artifact@v4
        with:
          name: ArkadasOdasi-Windows-Native
          path: publish/ArkadasOdasi-win-x64/**
